import { createStitches } from "@stitches/react";

export const styles = {
  theme: {
    colors: {
    },
    shadows: {
    },
    space: {
    },
    fontWeights: {
    },
    sizes: {
    },
    fontSizes: {
    },
    fonts: {
    },
    radii: {
    },
  },
  media: {
  },
};

export const {
  config,
  createTheme,
  css,
  getCssText,
  globalCss,
  styled,
  theme,
} = createStitches(styles);